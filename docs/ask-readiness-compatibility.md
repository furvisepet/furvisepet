# Ask readiness compatibility
Production migration 20260906064339_ask_readiness_compatibility applied successfully.
The previous browser function allowlist omitted three reviewed Ask readers. The care authority check expected public,pg_temp for the semantic wrapper, now using pg_catalog. This migration changes only readiness checks, not runtime grants, source evidence or owner predicates.
Existing failures are preserved. Reader admission requires the exact reviewed signature, owner, normalized function-definition fingerprint and authenticated-only EXECUTE. CRLF is normalized to LF. Future reader definition changes require updating the readiness fingerprint with matching tests; PostgreSQL formatter/version changes may also require review.
Real disposable PostgreSQL reproduced both failures. Eight assertions passed: failing baseline; clean fixed state; rejection of anon grant, service-role grant, reader path drift, unexpected public definer, semantic-writer path drift; clean restored state. All test changes and the synthetic migration receipt rolled back. First attempts exposed absent local migration bookkeeping, then CRLF fingerprint mismatch; both harness/portability issues were corrected before production application.
Production verification after applying the exact tested SQL returned contract_version=2, failed_checks=[] for all eight new migration names. Migration version aligned with repository. No application code or production user records modified. No live model or authenticated browser acceptance claimed.
SQL-only change: application suite/typecheck/lint not rerun; prior release suite 2286 passed is separate evidence. Staged diff check passed. Test instructions: copy migration to /tmp/ask-readiness-fix.sql and execute supabase/tests/ask_readiness_compatibility.sql with psql ON_ERROR_STOP on the disposable pre-fix database. Never run the adversarial test against production.
