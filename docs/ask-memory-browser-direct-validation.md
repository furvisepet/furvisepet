# Memory browser validation: direct desktop recovery

Direct Remote Desktop Commander execution succeeded on September 6 UTC. A fresh installed agent-browser Chrome session opened about:blank and evaluated 1+1 as2; that session was then closed. No user browser profile or security setting was changed. The runner-specific startup failure remains unexplained; it is not a general Chrome inability to execute JavaScript.

The prepared CDP driver then ran unchanged production components in installed Chrome with real React19/ReactDOM StrictMode. Two fixture defects were reproduced and corrected: invalid freshness_class stable replaced by supported medium_lived, and synthetic getBrowserSupabase explicitly exported from the extracted module. No production source changed.

All11 assertions passed: scoped loaders/projection; legacy Forget-only controls; correct legacy mutation/pet/ID; colliding canonical ID retained; canonical PATCH and real idempotency header; corrected same-key editor reopening; pending-auth pet switch; account principal mismatch; delayed old-account mutation completion; stale loader completion; no uncaught errors or unhandled rejections. Minimum free RAM on first passing run8.40GiB. Driver returned exit0, stopped its own browser/server and cleaned its own generated profile.

Reproduce from this worktree: node --max-old-space-size=2048 scripts/audits/ask-memory-browser.mjs. Scoped ESLint covers driver and fixture. Repeat-run output is stored at C:/Users/gwara/furvise-browser-direct-passed.log. Initial failure output C:/Users/gwara/furvise-browser-direct.stdout.log records the invalid fixture class. No default-suite rerun is needed for production changes because there were none; preceding default suite2281passed remains separate Node evidence.

Boundary: production memories-page reexport/page, actual React effects/cards, actual remembered-details projection, extracted real profile/memory loaders and real client idempotency execute. Auth hook, Next params/navigation, storage/fetch and visual shell use synthetic fixture doubles. This is NOT deployed Next routing, live auth, API route/DB/RLS/concurrency, visual/accessibility or paid-feature acceptance. Those rollout gates remain open.

Previous runner report/logs and rejected-cleanup profile tests/.memory-browser-V8rc2G remain preserved. That old profile is explicitly excluded from staging; no attempt was made to bypass its cleanup rejection. Runner state remains blocked and was not rewritten; direct recovery is recorded here. Lifetime audit expectations and production files are unchanged.
