# Retrieved weight comparison

Base 71d489f9189d1a47f5da38da4573deff2951c30c, codex/ask-remaining-reliability.
Exclusive lock C:/Users/gwara/furvise-remaining.lock held by PID 6304; release sentinel C:/Users/gwara/furvise-weight-comparison.release. No overlapping writer; AGENTS.md read.
Reproduced callback refusal despite three usable weight notes: C:/Users/gwara/furvise-weight-before.log.

Implemented a server-computed comparison of the retrieved subset. For a single authorized pet's earliest/latest weight question, complete positive named-pet kilogram statements supply integer-gram arithmetic. The answer identifies dates/values and explicitly says these are retrieved reports, not verified lifetime endpoints or complete correction coverage. It does not populate verifiedFacts or change any completeness state.
Unsupported units, qualified or mixed statements, nonstandard titles, future/invalid dates and conflicting simultaneous measurements prevent this calculation. Exact represented text/date, source membership and correction provenance are checked again before rendering. Detached numeric/date metadata cannot override those spans. Existing broader coverage-loss and source-change policies still run first.

Verification: full suite 2,246 passed; 12 new callback cases passed; typecheck and diff checks passed. Lint passed with the two existing warnings in persist-learnings.ts. Unchanged lifetime audit: 13 pass / 4 fail.
The weight audit checks for the delta text only. Its passing result MUST NOT be described as proof of complete lifetime endpoints. The output deliberately retains that limitation.
Logs: C:/Users/gwara/furvise-weight-verified.log, furvise-weight-focused.log, furvise-weight-typecheck.log, furvise-weight-lint.log, furvise-weight-audit.log.
An initial typecheck caught a nonexistent weight care category; extraction now uses retrieved note/title evidence and the new fixtures use the real general category. No schema change was made.

Limits: narrow English kilogram statements with up to three decimal places; no unit conversion, general clinical extraction, growth assessment or complete lifetime certificate. The existing bounded lexical retrieval may miss other formulations and the answer says so through its subset/correction qualification. No browser, real database or live-provider run is claimed; callback tests mock those dependencies.
This checkpoint accompanies the local implementation commit; git log supplies its exact ID. Release the lock after clean-worktree verification. No Docker, migrations, push, merge or deployment.
