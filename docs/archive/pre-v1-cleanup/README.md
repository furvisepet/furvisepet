# Preserved pre-cleanup artifacts

These twelve files were previously tracked directly under `tmp/`. They are
historical build/test output, test-name inventories, a tool transcript and a
captured favicon. None has an application, test, script or deployment consumer.
They are archived byte-for-byte, not treated as current launch validation.

For each file here, its former path was `tmp/<same filename>`. Old paths inside
the evidence are deliberately unchanged. The v1 file manifest records the
original hashes and this relocation. Current temporary output is ignored.

All existing benchmark reports, frozen questions, grades and results under
`docs/` remain in their original locations and are unchanged.
