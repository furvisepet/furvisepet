# Ask V1 stabilization

Base: 8059d11d69aed38741d491423f67925a30cbac08.
Branch: codex/ask-v1-stabilization.
Worktree: C:/Users/gwara/furvise-ask-v1-stabilization.
Exclusive lock owner: PowerShell PID47120, held across edits; revalidate process identity and handle before resuming.
Python edit process: PID49300. No additional Codex worker started.

Reproduced three callback failures before edits (furvise-v1-before.log in user's Temp directory): undated reference selection; undated period selection; read-only recall with unsupported terminal recovery triggering a paid repair and then failure.
Implemented bounded strategy normalization after ownership/date/ordinal validation, and read-only recovery neutralization before optional provider repair. Independent downstream gates remain.
Focused regression verification completed: furvise-v1-focused.log.

Still outstanding: conversational synthesis architecture, comprehensive budget-aware recovery, full verification and bounded live acceptance. No live provider calls made; authorized test cap is USD5 total. No production, database, push or deployment changes.

## Verified reliability increment

Three new callback reproductions: 0/3 before, 3/3 after. Combined focused suites: 77/77. Default suite including the new callback wrapper: 2292/2292 passed. Typecheck, lint (two unchanged persist-learnings warnings), production build and diff check passed. Validation logs: C:/Users/gwara/AppData/Local/Temp/furvise-v1-{before,focused,full,typecheck,lint,build}.log.
The two old assertions expecting undated selection rejection were updated to expect normalized summary selection. Existing valid dated-source exclusion and invalid vocabulary assertions remain. Original lifetime audit expectations were not edited or rerun.

The extra recovery call is reproduced locally, not proven to be the exact caller in the historical production budget-exhaustion event. This change suppresses only read-only recall recovery repair; mixed-turn recovery validation and other retry paths remain.
No live provider or database tests; test spending is USD0 of USD5 authorized. No push, merge or deployment.

## Remaining architecture work

Natural history synthesis is NOT implemented by this increment. attributedHistoryAnswer still validates per-source near-exact paraphrases and emits quotations when they fail. Do not describe the companion experience or V1 stabilization as complete. A subsequent change must preserve factual constraints while permitting direct, relevant synthesis, and test the actual final output across history, general guidance, grief and follow-up turns. Broader retry admission/recovery also remains. Keep this worktree and checkpoint as the starting point; recheck Git and processes and acquire the common exclusive lock before editing. Do not restart any old runner queue.

## Composition implementation in progress

Resumed from51a29c2 with clean worktree; common lock held by PowerShell51364, Python editor49448. No additional Codex or runner started. Reproduced natural summary replaced by full-note quotes (furvise-composition-before.log).
Added a model-proposed historyNarrative, independently source-scoped optional semantic review, in-memory review receipt, and validation integration. Dialogue context is separate from medical evidence. Important architecture change: Ask maximum calls increased from2 to3 for interpretation+answer+one historical review; repairs share that maximum. Review rejection/admission/transport failure falls back without failing the completed answer. Review is probabilistic, not a proof of entailment. Ordinary turns do not invoke it.
Next: focused adversarial/callback coverage, full checks and live synthetic acceptance. Live budget remains USD5; spent USD0 so far. No remote or DB changes. Do not resume edits without fresh process/lock inspection.

## Live verification milestone
Live model calls on synthetic records exposed a wrong event date, an inflected-search miss, an unnecessary named-topic clarification, and death misclassified as symptom recovery. Added source date/quantity anchor filtering, shared search stemming, bounded literal named-topic read recovery, and existing server loss classification before recovery repair. Prior assistant prose is excluded from dialogue context; recent user turns are separately labeled for reference/tone. Ordinary provider calls remain capped at2; only optional historical review may consume slot3. Model review is probabilistic and never authorizes writes.

At this checkpoint recorded cost USD0.425308, conservative cumulative reservations USD2.143883, authorization USD5. Budget ledger persists across runs and stops before USD4.50 reserved. Local Chrome fixture passed6 checks; production build passed before latest grief metadata change. Final full/live verification is pending. One earlier live subject mismatch remains unreproduced; do not claim universal recovery. Process-aware lock51364 remains held, editor49448. No DB/Docker/remote mutation.

## Composition verified for local review
The earlier in-progress entries are historical. Final implementation is described in docs/ask-reviewed-composition-report.md. Final checks: 2293 default tests passed; 21 new focused cases included through their wrapper; typecheck/build/diff passed; lint has two existing warnings. Real Chrome fixture passed6 checks. Live targeted causation/Luna/grief turns completed; token-cost estimate USD0.68964975 across79 calls, conservative reservations USD3.46226175 of USD5 authorization. No more live tests planned. Reviewer rejection can still produce quotation fallback; no universal correctness or launch-readiness claim.

Current lock owner PowerShell51364, editor49448. Final verification process45548 completed; live process50548 should be confirmed completed before releasing lock. No background runner was started. Next: commit this isolated change, export patch, update shared checkpoint, release owned lock. No push/merge/deployment or DB operations.

Completion handoff: the shared supervisor checkpoint records the resulting commit and lock release after this report is committed. Resume from that exact commit, not an earlier in-progress entry.
